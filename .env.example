# Web3Auth Configuration
NEXT_PUBLIC_WEB3AUTH_CLIENT_ID=

# API URLs
# Development: Used when running `npm run dev` (NODE_ENV=development)
NEXT_PUBLIC_LOCAL_API_URL=http://localhost:3001/api

# Production: Used when running `npm run build` or `npm start` (NODE_ENV=production)
NEXT_PUBLIC_REMOTE_API_URL=https://your-production-api.com/api